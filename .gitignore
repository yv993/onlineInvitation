# dependencies / build
node_modules/
.next/
out/

# the server's living data — links, RSVPs, orders, the couples' uploads.
# NEVER in the repo: it is user content, and it changes under the server.
data/

# secrets
.env.local
.env*.local

# OS noise
.DS_Store
Thumbs.db
tsconfig.tsbuildinfo
out/

# throwaway verification probes (fake KV server, headless checks). Each run
# writes its own; none of them is part of the product.
/scratchpad/

.vercel
.env*
